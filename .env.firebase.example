# Firebase Admin SDK Configuration
# These variables are required for push notifications to work.
# If not provided, push notifications will be disabled but the app will continue to work.

# To get these values:
# 1. Go to your Firebase Console
# 2. Select your project
# 3. Go to Project Settings (gear icon)
# 4. Go to Service accounts tab
# 5. Click "Generate new private key"
# 6. Download the JSON file and extract the values

FIREBASE_PROJECT_ID="your-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour\nPrivate\nKey\nHere\n-----END PRIVATE KEY-----"

# Note: Make sure to replace \n with actual newlines in the private key, or keep them as \n
# The code will automatically convert \n to newlines when reading the environment variable